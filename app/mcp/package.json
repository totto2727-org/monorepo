{
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@cloudflare/vite-plugin": "catalog:",
    "@cloudflare/workers-types": "catalog:",
    "@hono/mcp": "catalog:",
    "@modelcontextprotocol/sdk": "catalog:",
    "@tailwindcss/cli": "catalog:",
    "@totto/function": "catalog:",
    "daisyui": "catalog:",
    "dprint": "catalog:",
    "drizzle-kit": "catalog:",
    "drizzle-orm": "catalog:",
    "hono": "catalog:",
    "tailwindcss": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "zod": "catalog:"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.0.0"
  },
  "imports": {
    "#@*": "./app*"
  },
  "name": "mcp",
  "private": true,
  "scripts": {
    "build": "vite build",
    "check": "tsc",
    "db:apply": "wrangler d1 migrations apply mcp",
    "db:generate": "drizzle-kit generate",
    "deploy": "wrangler deploy",
    "dev": "vite dev",
    "prebuild": "$npm_execpath run -p /prebuild:.*/",
    "prebuild:tailwindcss": "tailwindcss -i ./app/admin/ui/layout/tailwind.css -o ./public/asset/tailwind.css",
    "prebuild:wrangler": "wrangler types",
    "preview": "wrangler dev --test-scheduled",
    "start": "wrangler dev --test-scheduled"
  },
  "type": "module"
}
