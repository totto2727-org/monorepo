# TC-015 evidence: YAML parse after placeholder substitution

## Original template (first 10 lines)
identifier: { { identifier } }
started_at: { { started_at } }
updated_at: { { updated_at } }

status: { { status } } # active | completed | blocked
current_step: { { current_step } }

completed_steps: []
  # - step: <ステップ番号と名称>
  #   completed_at: <ISO8601>

## After placeholder substitution (PLACEHOLDER token)
identifier: PLACEHOLDER
started_at: PLACEHOLDER
updated_at: PLACEHOLDER

status: PLACEHOLDER # active | completed | blocked
current_step: PLACEHOLDER

completed_steps: []
  # - step: <ステップ番号と名称>
  #   completed_at: <ISO8601>

## YAML parse result (uvx --from pyyaml python3, sandbox-disabled run)
roadmap key exists: True
roadmap value: None

## Conclusion
Template contains Mustache placeholders ({{...}}) which are not strict YAML.
After placeholder substitution (= post-instantiation state), the YAML is parseable
and the `roadmap` top-level key exists with null value, satisfying SC-6.

## Verification command (sandbox-disabled execution)
uvx --from pyyaml python3 -c "import yaml; d=yaml.safe_load(open('/tmp/claude/progress-substituted.yaml')); print('roadmap key exists:', 'roadmap' in d); print('roadmap value:', d.get('roadmap'))"
