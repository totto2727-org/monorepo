[formatter.go]
command = "gofmt"
options = ["-w"]
includes = ["*.go"]

[formatter.nix]
command = "nixfmt"
includes = ["*.nix"]

[formatter.vite-plus]
command = "vp"
options = ["check", "--no-error-on-unmatched-pattern", "--fix"]
includes = ["*"]
