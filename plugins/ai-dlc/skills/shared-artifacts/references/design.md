# Reference: `design.md` の書き方

## 目的

実装前に、**アーキテクチャ・コンポーネント構成・API 設計・データフロー・代替案**を体系化する。Construction 中の implementer がこのドキュメントだけで実装判断できる詳細度まで書く。サイクル固有の判断はここで完結させる（プロジェクト横断の意思決定のみ別途 ADR を起票）。

## 作成者 / 作成タイミング

- **作成者:** `architect` Specialist（単一インスタンス、反復改善）
- **作成ステップ:** Inception Step 3 (Design)
- **承認:** ユーザー承認必須（Artifact-as-Gate-Review）

## ファイル位置

`docs/ai-dlc/<identifier>/design.md`

## 各セクションの書き方

### 設計目標と制約

`intent-spec.md` からの**引用**で冒頭を固める。目的・成功基準・主要制約を設計判断の起点として明示。

### アプローチの概要

**1–3 段落**で全体像を述べる。詳細はコンポーネント構成以降で展開する。「なぜこのアプローチか」の中核理由を必ず含める。

### コンポーネント構成

- 主要コンポーネント / モジュール / 層の一覧と役割
- 関係性を Mermaid 図で可視化することを推奨
- 既存コンポーネントとの境界・接続点を明示

### 主要な型・インターフェース

**実装可能な具体レベル**まで書く。TypeScript / Rust / Go などの型定義で示す。API の契約を型で表現できるなら積極的に使う。

### データフロー / API 設計

API エンドポイント表を記述:

| Method | Path | Description | Request | Response |
| ------ | ---- | ----------- | ------- | -------- |

データフローは図（シーケンス図等）で表現する。

### 代替案と採用理由

**必ず 2–3 個の代替案を比較**する。1 案しか書かれていない設計書は検討が浅い。

| 案  | 概要 | 採用 / 却下 | 理由 |
| --- | ---- | ----------- | ---- |
| A   | ...  | 採用        | ...  |
| B   | ...  | 却下        | ...  |

### 想定される拡張ポイント

将来の拡張のために今回の設計で確保している余地を明示。YAGNI 原則に反しない範囲で。

### 運用上の考慮事項

以下を全て記述（該当なしは「N/A」と明示）:

- 監視 / 観測
- 移行 / 切替
- ロールアウト
- ロールバック
- セキュリティ
- パフォーマンス予測

### プロジェクト横断 ADR への参照

サイクル中にプロジェクト全体に及ぶ決定が発生した場合のみ、ADR へのリンクを張る。

### Task Decomposition への引き継ぎポイント

Step 4 の `planner` が使うヒント。タスク分割の粒度目安・並列性の手掛かりを明示。

## 品質基準

| ✅ よい                                         | ❌ 悪い                                  |
| ----------------------------------------------- | ---------------------------------------- |
| implementer がこの設計書だけで実装判断できる    | 抽象的で実装に落とせない                 |
| 代替案の比較が 2–3 個ある                       | 採用案のみ記述                           |
| 運用上の考慮事項が全カテゴリ埋まっている        | セキュリティ・パフォーマンスが抜けている |
| Research Notes の含意が設計判断に反映されている | 調査を無視した設計                       |

## ADR 起票の判定基準

`design.md` 内で完結させるか、別途 ADR を起票するかの判定:

| ADR 対象（`adr` スキルで起票）   | ADR 対象外（`design.md` 内で完結）      |
| -------------------------------- | --------------------------------------- |
| プロジェクト全体で Effect を採用 | この機能のキャッシュ戦略を LRU に       |
| 全サービスで gRPC を使う         | この API のページネーションは cursor 型 |
| 認可レイヤを OpenFGA に統一      | この画面のバリデーションは zod で書く   |

- 他の機能・他のチーム・将来のサイクルにも影響する → ADR
- 本サイクル内で完結する判断 → `design.md` 内にとどめる

## 関連成果物

- **入力:** `intent-spec.md` / `research/*.md` 全件
- **出力先:** `task-plan.md`（planner）、全 Construction / Verification 成果物
- **ADR 連携:** 起票した場合 `progress.yaml.artifacts.external_adrs` に記録、`design.md` からリンク
