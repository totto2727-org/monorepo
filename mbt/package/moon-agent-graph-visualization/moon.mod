name = "totto2727/moon-agent-graph-visualization"

version = "0.0.0"

readme = "README.md"

repository = "https://github.com/totto2727-org/monorepo"

license = "MIT"

keywords = [ "agent", "graph", "visualization", "mermaid", "moonbit" ]

description = "Visualization helpers for compiled Moon Agent Graph workflows"

import {
  "moonbitlang/async@0.20.1",
  "totto2727/moon-agent-graph@0.0.0",
}

preferred_target = "native"

supported_targets = "native"

source = "src"
