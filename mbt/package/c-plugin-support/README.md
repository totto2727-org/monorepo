# c-plugin-support

Local MoonBit support module used by `mbt/app/c-plugin`.

This module is registered through `moon.work`; `mbt/app/c-plugin/moon.mod` imports `totto2727/c-plugin-support@0.1.0`, which resolves to this local module in the workspace.
