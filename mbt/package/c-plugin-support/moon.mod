name = "totto2727/c-plugin-support"

version = "0.1.0"

preferred_target = "native"

supported_targets = "native"

readme = "README.md"

repository = "https://github.com/totto2727-org/monorepo"

license = "MIT"

keywords = [ "c-plugin", "cli", "support" ]

description = "Local support utilities for the native MoonBit c-plugin CLI"

options(
  source: "./src",
)
