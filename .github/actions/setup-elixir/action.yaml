name: Setup Elixir
description: Fetch Mix dependencies with the Nix-managed Elixir toolchain.

runs:
  using: composite
  steps:
    - name: Fetch Mix dependencies
      shell: bash
      run: |
        eval "$(nix print-dev-env "$GITHUB_WORKSPACE#default")"
        mix deps.get
