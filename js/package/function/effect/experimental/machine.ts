/**
 * Re-exports of Effect's experimental `Machine` module.
 *
 * @example
 * ```ts
 * import { Machine } from "@totto/function/effect/experimental/machine";
 * ```
 *
 * @module
 */

export { Machine } from '@effect/experimental'
